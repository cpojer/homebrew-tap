name: test

on:
  pull_request:
  push:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Audit cask
        run: |
          brew tap nkzw-tech/tap "$GITHUB_WORKSPACE"
          brew audit --cask nkzw-tech/tap/codiff
          brew style --cask nkzw-tech/tap/codiff
